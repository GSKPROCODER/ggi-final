'use client';

import AgentChat from '@/pages/AgentChat';

/**
 * Conversational agent intelligence page. Runs conversational queries over 
 * databases using the DeepMind-critic LangGraph agent.
 */
export default function Page() {
  return <AgentChat />;
}
