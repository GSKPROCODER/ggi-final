'use client';

import Settings from '@/pages/Settings';

/**
 * Settings configuration page. Enables profile editing, API configurations,
 * and system preference tuning.
 */
export default function Page() {
  return <Settings />;
}
