'use client';

import UploadData from '@/pages/UploadData';

/**
 * Upload dataset page. Provides drag-and-drop CSV uploads, schema mapping,
 * and chunked batch analysis progress tracking.
 */
export default function Page() {
  return <UploadData />;
}
