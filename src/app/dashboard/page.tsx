'use client';

import Dashboard from '@/pages/Dashboard';

/**
 * Overview dashboard page. Runs dynamic KPI cards, timeseries analytics,
 * and emotion percentages.
 */
export default function Page() {
  return <Dashboard />;
}
