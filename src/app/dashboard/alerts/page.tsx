'use client';

import Alerts from '@/pages/Alerts';

/**
 * Anomaly alerts page. Aggregates identified risk indicators, spikes,
 * and high-severity semantic concerns in an interactive panel.
 */
export default function Page() {
  return <Alerts />;
}
